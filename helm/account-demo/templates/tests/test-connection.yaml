apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "account-demo.fullname" . }}-test-connection"
  labels:
    {{- include "account-demo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "account-demo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
